[build-system]
build-backend = "scikit_build_core.build"
requires = [
    "cython>=3.0.0",
    "scikit-build-core",
]

[project]
name = "pkg2"
version = "1.0.0"

[tool.scikit-build]
ninja.make-fallback = false
