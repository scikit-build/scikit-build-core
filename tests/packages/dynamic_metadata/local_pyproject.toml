[build-system]
requires = ["scikit-build-core"]
build-backend = "scikit_build_core.build"

[project]
name = "dynamic"
dynamic = ["version"]

[tool.scikit-build]
experimental = true

[tool.scikit-build.metadata.version]
provider = "version.nested"
provider-path = "plugins/local"
