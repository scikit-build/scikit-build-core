[build-system]
requires = [
    "scikit_build_core",
    "setuptools",
    "pybind11",
]
build-backend = "scikit_build_core.setuptools.build_meta"

[project]
name = "cmake-example"
version = "0.0.1"
requires-python = ">=3.8"

[tool.scikit-build]
cmake.source-dir = "."
