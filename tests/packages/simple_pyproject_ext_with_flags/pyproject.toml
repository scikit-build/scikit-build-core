[build-system]
requires = [
    "scikit_build_core",
]
build-backend = "scikit_build_core.build"

[project]
name = "CMake.Example"
version = "0.0.1"
requires-python = ">=3.7"

[tool.scikit-build]
wheel.py-api = "py3"
