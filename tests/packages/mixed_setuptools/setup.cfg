[metadata]
name = mixed-setuptools
version = 3.1.4

[options]
zip_safe = False
python_requires = >=3.7
packages = find:
package_dir =
    =src

[options.packages.find]
where = src

[build_cmake]
source_dir = .
